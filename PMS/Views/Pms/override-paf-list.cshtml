@{
    Layout = null;
}



<section class="content ">
    <div class="panel panel-solid">
        <div class="panel-body" data-ng-init="onInit()">
            <h2 class="page-header">List of Employees</h2>
       
            <!--Prompt Message after save-->
            <div class="callout callout-info" ng-show="msgOK">
                <h4><span class="rjg-check-1"></span> Success</h4>
                <p><strong>User details and right's successfully update. </strong>  </p>
            </div>
            <div class="callout callout-danger" ng-show="msgInvalid">
                <h4><span class="rjg-cancel"></span> Invalid</h4>
                <p>{{errorMsg}}</p>
            </div>
            <!--End Prompt Message-->

            <div data-ng-repeat="x in pafMaster" data-ng-show="onViewDetails(x.pmsEmployeeDtl)">
                <h3>{{onType(x.pmsLstType)}}</h3>
                <table class="table table-condensed table-hover" ng-if="x.pmsLstType==1">
                    <tr>
                        <th></th>
                        <th>Employee ID</th>
                        <th>Employee Name</th>
                        <th>Department</th>
                        <th>Position</th>
                    </tr>
                    <tr data-ng-repeat="xx in x.pmsEmployeeDtl">
                        <td>
                            <a class="pointer" data-ng-show="hasOpen(xx)" data-ng-click="onOpenOveridePaf(xx)">Open</a>
                            <span data-ng-show="!hasOpen(xx)">N/A</span>
                        </td>
                        <td>{{xx.empId}}</td>
                        <td>{{xx.empName}}</td>
                        <td>{{xx.department}}</td>
                        <td>{{xx.position}}</td>
                    </tr>
                </table>
                <!-- pmsLstType=2-->
                <table class="table table-condensed table-hover" ng-if="x.pmsLstType==2">
                    <tr>
                        <th></th>
                        <th>Employee ID</th>
                        <th>Employee Name</th>
                        <th>Department</th>
                        <th>Position</th>
                    </tr>
                    <tr data-ng-repeat="xx in x.pmsEmployeeDtl  | filter:search:strict">
                        <td>
                            <a class="pointer" data-ng-show="hasOpen(xx)" data-ng-click="onOpenOveridePaf(xx)">Open</a>
                            <span data-ng-show="!hasOpen(xx)">N/A</span>
                        </td>
                        <td>{{xx.empId}}</td>
                        <td>{{xx.empName}}</td>
                        <td>{{xx.department}}</td>
                        <td>{{xx.position}}</td>
                    </tr>
                </table>
                <!-- pmsLstType=3-->
                <table class="table table-condensed table-hover" ng-if="x.pmsLstType==3">
                    <tr>
                        <th></th>
                        <th>Employee ID</th>
                        <th>Employee Name</th>
                        <th>Department</th>
                        <th>Position</th>
                    </tr>
                    <tr data-ng-repeat="xx in x.pmsEmployeeDtl  | filter:search:strict">
                        <td>
                            <a class="pointer" data-ng-show="hasOpen(xx)" data-ng-click="onOpenOveridePaf(xx)">Open</a>
                            <span data-ng-show="!hasOpen(xx)">N/A</span>
                        </td>
                        <td>{{xx.empId}}</td>
                        <td>{{xx.empName}}</td>
                        <td>{{xx.department}}</td>
                        <td>{{xx.position}}</td>
                    </tr>
                </table>
            </div>


        </div>
    </div>
</section>


<!-- modal view for applicant remove prompt-->
<div id="modal-panel-override-paflist" class="popup-basic  popup-lg bg-none mfp-with-anim mfp-hide">
    <div class="panel">
        <div class="panel-heading">
            <span class="panel-title"><i class="glyphicon glyphicon-tasks"></i>Paf List</span>
        </div>
        <div class="panel-body">
            <p class="text-danger">{{stopnote}}</p>
            <table class="table table-condensed table-hover">
                <tr>
                    <th></th>
                    <th>Covered</th>
                    <th>Created Date</th>
                    <th>Status</th>
                </tr>
                <tr data-ng-repeat="x in panoList | filter:{Status:'O'}">
                    <td>

                        <button class=" btn btn-xs btn-primary" data-ng-click="onOverride(x.hashcode,x.Status)" >Override</button>

                    </td>
                    <td>{{x.COVERED_FROM | date:'MM/dd/yyyy'}}&nbsp;to&nbsp;{{x.COVERED_TO | date:'MM/dd/yyyy'}}</td>
                    <td>{{x.DT_OF_AP | date:'MM/dd/yyyy'}}</td>
                    <td>{{x.Status=='C' ? 'Close':'Open'}}</td>
                </tr>
            </table>
        </div>
        <div class="panel-footer text-right">
            <button class="btn" data-ng-click="closeModalForm()">Cancel</button>
        </div>
    </div>
</div>


